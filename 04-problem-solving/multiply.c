// 02 problem: you will be given  two integer A and B you need to give their multiplication
#include <stdio.h>

int main() {
    int A, B, sum;

    printf("Enter two integers: ");
    scanf("%d %d", &A, &B);

    // Calculate the product
     sum = A * B;

    // Display the result
    printf(" %d \n", sum);

    return 0;
}
